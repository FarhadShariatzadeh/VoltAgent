import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "VoltAgent — Your Personal Energy Manager",
  description:
    "AI-powered energy monitoring that forecasts your bill, optimizes appliance timing, and helps you avoid expensive rate tiers — delivered by email and SMS.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
